overwrite: true
# schema: "http://localhost:4000/api/graphql"
schema: "https://api-balance-track.vercel.app/api/graphql"
documents: "src/**/*.graphql"
generates:
  ./src/generated/index.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
